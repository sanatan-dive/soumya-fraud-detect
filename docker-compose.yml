services:
  zookeeper:
    image: zookeeper:latest
    container_name: zookeeper
    ports:
      - "2181:2181"
    networks:
      - fraud-detection-network

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      # Advertise hostname reachable to other containers (backend) instead of localhost
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    networks:
      - fraud-detection-network

  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: fraud_detection
    volumes:
      - mongodb_data:/data/db
    networks:
      - fraud-detection-network

  backend:
    build: ./backend
    container_name: fraud-detection-backend
    depends_on:
      - kafka
      - mongodb
    ports:
      - "5001:5000"
    environment:
      MONGODB_URI: mongodb://mongodb:27017/fraud_detection
      # Match backend server.js expectation (KAFKA_BROKERS)
      KAFKA_BROKERS: kafka:9092
      PORT: 5000
    networks:
      - fraud-detection-network
    volumes:
      - ./backend:/app
      - /app/node_modules

networks:
  fraud-detection-network:
    driver: bridge

volumes:
  mongodb_data:
